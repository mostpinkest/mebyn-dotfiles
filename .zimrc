# Start configuration added by Zim install {{{
#
# This is not sourced during shell startup, and it's only used to configure the
# zimfw plugin manager.
#

#
# Modules
#

# Sets sane Zsh built-in environment options.
zmodule environment
# Applies correct bindkeys for input events.
zmodule input
# Sets a custom terminal title.
zmodule termtitle
# Utility aliases and functions. Adds colour to ls, grep and less.
zmodule utility

#
# Prompt
#

# Using Powerlevel10k for prompt; remove asciiship stack
# (duration-info, git-info, asciiship) to avoid duplication.

#
# Completion
#

# Additional completion definitions for Zsh.
zmodule zsh-users/zsh-completions --fpath src

# Enables and configures smart and extensive tab completion.
zmodule completion

# FZF-driven completion UI (load after compinit)
zmodule Aloxaf/fzf-tab

#
# Modules that must be initialized last
#

# Fish-like syntax highlighting for Zsh. Must load after completion.
# If using zsh-users/zsh-syntax-highlighting, switch the module below.
zmodule zdharma-continuum/fast-syntax-highlighting
# History substring search (Up/Down arrows integration).
zmodule zsh-users/zsh-history-substring-search
# VI mode should initialize before autosuggestions so autosuggest can bind on top.
# (zvm-specific keybindings are finalized via zvm_after_lazy_keybindings in ~/.zshrc)
zmodule jeffreytse/zsh-vi-mode
# Autosuggestions should be the last interactive widget hook.
zmodule zsh-users/zsh-autosuggestions
# }}} End configuration added by Zim install

# P10k theme
zmodule romkatv/powerlevel10k

# zmodule zdharma-continuum/history-search-multi-word  # redundant with Atuin and substring-search; removed

# NVM for ZSH
zmodule lukechilds/zsh-nvm

# Oh My Zsh Git helpers and aliases
zmodule ohmyzsh/ohmyzsh --source lib/git.zsh
zmodule ohmyzsh/ohmyzsh --source plugins/git/git.plugin.zsh

# fzf-powered Git helpers
zmodule wfxr/forgit
